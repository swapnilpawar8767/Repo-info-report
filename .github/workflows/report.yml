name: Daily GitHub Repo Report

on:
  schedule:
    - cron: "0 9 * * *"  # Daily at 09:00 UTC (2:30 PM IST)
  workflow_dispatch:

jobs:
  daily-report:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install requests

      - name: Generate and send report
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TEAMS_WEBHOOK_URL: ${{ secrets.TEAMS_WEBHOOK_URL }}
        run: |
          python << 'EOF'
          import requests
          import os
          import datetime

          repos = [
              "swapnilpawar8767/Repo-info-report"  # Add more repos here if needed
          ]

          headers = {
              "Authorization": f"token {os.environ['GITHUB_TOKEN']}",
              "Accept": "application/vnd.github.v3+json"
          }

          report_date = datetime.date.today()
          report_text = f"📊 Daily GitHub Repo Report - {report_date}\n"

          separator = "──────────────────────────────"

          for repo in repos:
              response = requests.get(
                  f"https://api.github.com/repos/{repo}/issues?state=all&per_page=5&sort=updated",
                  headers=headers,
              )
              issues = response.json()

              for issue in issues:
                  if "pull_request" in issue:
                      continue  # skip PRs

                  title = issue.get("title", "")
                  status_raw = issue.get("state", "").lower()
                  status = "🆕 Open" if status_raw == "open" else "Closed"
                  updated_at = issue.get("updated_at", "")[:10]

                  due_date = "-"
                  milestone = issue.get("milestone")
                  if milestone and milestone.get("due_on"):
                      due_date = milestone["due_on"][:10]

                  last_comment = "No comments"
                  if issue.get("comments", 0) > 0:
                      comments_response = requests.get(issue["comments_url"], headers=headers)
                      comments = comments_response.json()
                      if comments:
                          last_comment = comments[-1].get("body", "").replace("\n", " ")
                          if len(last_comment) > 50:
                              last_comment = last_comment[:47] + "..."

                  report_text += f"\n{separator}\n"
                  report_text += f"📌 {title} ({repo})\n"
                  report_text += f"Last Comment :: {last_comment}\n"
                  report_text += f"Due Date     :: {due_date}\n"
                  report_text += f"Status       :: {status}\n"
                  report_text += f"Last Updated :: {updated_at}\n"

          report_text += f"\n{separator}\n"

          payload = {"text": report_text}
          resp = requests.post(os.environ["TEAMS_WEBHOOK_URL"], json=payload)
          resp.raise_for_status()
          print("✅ Report sent to Teams successfully!")
          EOF
